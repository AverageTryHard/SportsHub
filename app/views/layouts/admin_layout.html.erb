<!DOCTYPE html>
<html>
  <head>
    <title>Sports Hub - Admin</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <% if controller_name == 'categories' %>
      <%= javascript_pack_tag 'category' %>
    <% end -%>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  </head>

  <body style="margin: 0">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="container-xl p-0 w-100">
      <%= render :partial => 'common/header' %>
      <%= render :partial => 'title_new' %>
      <%= render :partial => 'admin/categories_header' %>
      <div class="row m-0">
        <div class="h-100 icon-container w-auto">
          <div class="list-group m-2 align-items-center">
            <i class="bi bi-check2-circle icon-nav"></i>
            <i class="bi bi-card-text icon-nav"></i>
            <%= link_to admin_languages_path, class: 'icon-nav' do %><i class="bi bi-globe"></i><% end %>
            <i class="bi bi-square icon-nav"></i>
            <i class="bi bi-share icon-nav"></i>
            <%= link_to admin_root_path, class: 'icon-nav' do %><i class="bi bi-person"></i><% end %>
            <%= link_to admin_categories_path, class: 'icon-nav' do %><i class="bi bi-diagram-3"></i><% end %>
            <%= link_to admin_teams_path, class: 'icon-nav' do %><i class="bi bi-people icon-nav"></i><% end %>
            <i class="bi bi-briefcase icon-nav"></i>
            <i class="bi bi-megaphone icon-nav"></i>
          </div>
        </div>
        <div class="col row p">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
