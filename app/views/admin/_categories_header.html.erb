<div class="row left">
  <div class="col-1">Home</div>
  <% (@categories || []).each do |category| %>
    <div class="col-1"><%= link_to category.name, admin_articles_path('category' => category) %></div>
  <% end %>
</div>